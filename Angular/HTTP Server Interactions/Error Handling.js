2 types of errors:  Expected and UnExpected


Unexpected Errors include:
  -Server is offline
  -Network in down  (user cannot reach their destination)
  -Unhandled exceptions


Expected Errors include:
  -"Not Found" (404) error:
      Example: user tries to interact with an item that another user JUST deleted

  -"Bad Request" (400) error:
      Example: this could be when user chooses username that is already taken
